# Noted Mechanisms

Acknowledged warnings, deferred fixes, and specialty mechanisms that
have existing mitigation in the codebase but need final resolution.

Each file documents: what the issue is, what mechanism already exists
to handle it, and when it should be resolved.

Resolution target: **after S33** (end of the current session roadmap).
