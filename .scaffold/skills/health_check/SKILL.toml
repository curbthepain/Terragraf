[skill]
name = "health_check"
version = "0.1"
description = "Full system diagnostic: structure, tests, analytics, environment"
entry = "run.py"
type = "diagnostic"

[triggers]
intents = ["health check", "system health", "scaffold status", "diagnose"]
commands = ["terra health"]

[deps]
python = [">=3.11"]
modules = []
